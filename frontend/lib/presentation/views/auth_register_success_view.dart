import 'package:flutter/material.dart';

class AuthRegisterSuccessView extends StatelessWidget {
  const AuthRegisterSuccessView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
